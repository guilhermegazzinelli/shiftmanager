<%= form_with(model: regional, local: true) do |form| %>
  <% if regional.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(regional.errors.count, "error") %> prohibited this regional from being saved:</h2>

      <ul>
      <% regional.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :regional_name %>
  </div>

  <div class="field">
    <%= form.label :operation_id %>
    <%= form.text_field :operation_id, id: :regional_operation_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
